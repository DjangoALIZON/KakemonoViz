services:

  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./php:/var/www/html
    depends_on:
      - db

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: etudiant
      POSTGRES_PASSWORD: etudiant
      POSTGRES_DB: projet
    ports:
      - "5432:5432"